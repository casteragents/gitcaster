import { PRODUCT } from "../lib/caster-copy";

export function CasterFooter() {
  return (
    <footer className="footer">
      {PRODUCT.name} alpha-local truth UI. Preview data is labeled. Proof-backed data requires evidence.
    </footer>
  );
}
