{
  "name": "@gitcaster/cli",
  "version": "0.1.0-alpha",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "gc": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node dist/cli.test.js",
    "check": "pnpm run build && pnpm run test",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@gitcaster/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
